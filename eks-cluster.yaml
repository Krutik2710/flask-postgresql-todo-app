apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: flask-todo-cluster
  region: eu-north-1

managedNodeGroups:
  - name: workers
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 3
    volumeSize: 20