apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: todo-app

spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 2Gi